///Modelo de un contador de clicks
///Atributo _contadorClicks iniciado en cero
class Contador {
  final int _contadorClicks;
  Contador([this._contadorClicks = 0]);

  int get contadorClicks => _contadorClicks;

}
